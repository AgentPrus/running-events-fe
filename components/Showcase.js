import styles from '@/styles/Showcase.module.css'

const Showcase = () => {
    return (
        <div className={styles.showcase}>
            <h1>Welcome to the Running Events</h1>
            <h2>Find the hottest races</h2>
        </div>
    );
};

export default Showcase;